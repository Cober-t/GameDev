-- RenderSystem = ECS.system({ pool = {"transform", "texture"} })-- Define a System that takes all Entities with a position and texture Component

-- function RenderSystem:draw()
--     -- for _, entity in ipairs(self.pool) do

--     -- end
-- end